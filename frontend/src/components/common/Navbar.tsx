import React from 'react';

const Navbar: React.FC = () => (
  <nav className="bg-gray-900 border-b border-gray-800 px-6 py-3">
    <span className="text-white font-semibold">Amdox ERP</span>
  </nav>
);

export default Navbar;
