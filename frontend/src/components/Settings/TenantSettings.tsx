import React from 'react';
const TenantSettings: React.FC = () => (
  <div className="bg-gray-800 rounded-xl p-6 border border-gray-700">
    <h2 className="text-white font-bold text-lg mb-4">Tenant Settings</h2>
    <p className="text-gray-400 text-sm">Tenant Settings — implementation by M2 (Auth/Settings).</p>
  </div>
);
export default TenantSettings;
